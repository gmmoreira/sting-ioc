# Sting - C# IoC container for learning purposes

## Build

Build docker image. The compiled assemblies are located in /usr/src/app/build directory.
Running the docker image will also run unit tests.
